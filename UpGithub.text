git add .
git commit -m "Revise"
git push origin main

1️⃣ git add .

代表把目前資料夾下所有修改、新增、刪除的檔案加入暫存區。

2️⃣ git commit -m "Revise"

執行後，檔案就從「暫存區」→ 進入 本地 Git 歷史 (HEAD)。

3️⃣ git push origin main

這一步會把本地的最新 commit 同步到 GitHub。
